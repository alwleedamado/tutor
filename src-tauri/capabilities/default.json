{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main window: core defaults only. All privileged operations go through validated application commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
